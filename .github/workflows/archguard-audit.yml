name: ArchGuard Architectural Audit

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  audit:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: 
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run ArchGuard AI Reviewer
        uses: paudang/archguard-ai@main
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
